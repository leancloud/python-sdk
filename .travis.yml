language: python
matrix:
  include:
    -python:"2.7"
     env: TEST_SUITE=test_2_7
    -python:"2.6"
     env: TEST_SUITE=test_2_6
   
sudo: false

# command to install dependencies
install:
  - "pip install -e .[test]"

# command to run tests
script: nosetests tests/TEST_SUITE
